export const prefix = '@@io';

export const CREATE = `${prefix}/create`;
export const MOUNT = `${prefix}/mount`;
export const DISMOUNT = `${prefix}/dismount`;
export const CONNECT = `${prefix}/connect`;
export const DISCONNECT = `${prefix}/disconnect`;
export const ON = `${prefix}/on`;
export const OFF = `${prefix}/off`;
export const ONCE = `${prefix}/once`;
export const SEND = `${prefix}/send`;
export const ACK = `${prefix}/ack`;
export const DEFAULTS = `${prefix}/defaults`;
